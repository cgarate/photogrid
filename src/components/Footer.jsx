import React from "react";

import {
  FooterContainer,
  FlexContainer,
  AvatarContainer,
} from "./footerStyles";

const Footer = () => {
  return (
    <FooterContainer>
      <FlexContainer>
        <AvatarContainer></AvatarContainer>
      </FlexContainer>
    </FooterContainer>
  );
};

export default Footer;
